import { styled } from "styled-components";
import Input from "./Input";

const FormTag = styled.form``;
const Form = () => {
  return (
    <FormTag>
      <Input />
    </FormTag>
  );
};

export default Form;
